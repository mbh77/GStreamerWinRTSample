<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Presentation.StreamingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid x:Name="controlGrid" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="IP" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{x:Bind ViewModel.Address, Mode=TwoWay}" Margin="5, 1" VerticalAlignment="Center" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Port" Width="auto" Height="auto" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.Port, Mode=TwoWay}" Margin="5, 1" VerticalAlignment="Center" />

            <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Start Streaming"
                    Click="{x:Bind ViewModel.OnStartStreamingClicked}" Margin="1" HorizontalAlignment="Stretch"/>
            <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="Stop Streaming" 
                    Click="{x:Bind ViewModel.OnStopStreamingClicked}"  Margin="1" HorizontalAlignment="Stretch"/>
        </Grid>
        <Grid x:Name="videoHost" Grid.Column="1" Background="Black">
            <Viewbox Stretch="Uniform">
                <SwapChainPanel x:Name="videoPanel" Width="1920" Height="1080" />
            </Viewbox>
        </Grid>
    </Grid>
</Page>
