<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <RootNamespace>MediaFrameworkProjection</RootNamespace>
    <RuntimeIdentifiers>win-x86;win-x64;win-arm64</RuntimeIdentifiers>
    <UseWinUI>true</UseWinUI>
    <Platforms>AnyCPU;x64</Platforms>
    <VersionPrefix>1.0</VersionPrefix>
    <YearMonth>$([System.DateTime]::UtcNow.ToString("yyMM"))</YearMonth>
    <Day>$([System.DateTime]::UtcNow.ToString("dd"))</Day>
    <Hour>$([System.DateTime]::UtcNow.ToString("HH"))</Hour>
    <Minute>$([System.DateTime]::UtcNow.ToString("mm"))</Minute>
    <UniqueKey>$([MSBuild]::Add($([MSBuild]::Add($([MSBuild]::Multiply($(Day), 2048)), $([MSBuild]::Multiply($(Hour), 64)))), $(Minute)))</UniqueKey>
    <Version>$(VersionPrefix).$(YearMonth).$(UniqueKey)</Version>    
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Windows.CsWinRT" Version="2.2.0" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.4188" />
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.7.250401001" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\MediaFramework\MediaFramework.vcxproj" />
  </ItemGroup>
  <PropertyGroup>
    <CSWinRTIncludes>MediaFramework</CSWinRTIncludes>
    <CsWinRTGeneratedFilesDir>$(OutDir)</CsWinRTGeneratedFilesDir>
  </PropertyGroup>
  <ItemGroup>
    <CsWinRTWinMDReference Include="$(OutDir)MediaFramework.winmd" />
  </ItemGroup>
</Project>